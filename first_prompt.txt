I am building a LangGraph-based job search agent. The full architecture, all decisions, and constraints are documented in SPEC.md and CLAUDE.md in this repository. Please read both files in full before doing anything else.

Summary: daily LangGraph pipeline fetching job listings from target company career pages, deduplicating against SQLite, scoring with Gemini 2.0 Flash, delivering a Gmail digest with apply links. Second weekly pipeline reads outcomes and rewrites the scoring prompt. GitHub Actions CI/CD. GitHub Codespaces for demos.

HARD CONSTRAINTS — follow exactly:
1. STUBS ONLY. I write all implementations. Create type signatures, docstrings, pass bodies only. Never implement logic unless I name a specific function and ask you to implement it.
2. PHASE BY PHASE. Start Phase 1 only. Wait for my confirmation before Phase 2.
3. NON-FATAL ERRORS. Catch per company/job, append to state["errors"], never crash the pipeline.
4. NO ORM. Raw sqlite3 stdlib only.
5. BATCH LLM. One Gemini prompt per run for all jobs.
6. PROMPT FROM FILE. Load from config/scoring_prompt.txt at runtime, never hardcode.
7. LANGCHAIN SDK. Use ChatGoogleGenerativeAI, not raw google-generativeai.
8. DEDUP BY ID. Use ATS job_id always, never title or URL.
9. DB PATH FROM ENV. Never hardcode data/jobs.db or data/demo.db. Always use settings.DB_PATH.

Start by:
1. Reading SPEC.md
2. Reading CLAUDE.md
3. Confirming you understand the architecture and all 9 constraints
4. Creating Phase 1 — all files and directories listed below. Create every folder and file shown, including all __init__.py files. For .py files: stubs only (type signatures, docstrings, pass bodies). For config/yaml files: populate with real content from SPEC.md.

FULL REPO STRUCTURE TO CREATE IN PHASE 1:

job-search-agent/
├── .github/
│   └── workflows/
│       ├── ci.yml
│       └── weekly_agent.yml
├── .devcontainer/
│   └── devcontainer.json
├── agent/
│   ├── __init__.py
│   ├── graph.py
│   ├── state.py
│   └── nodes/
│       ├── __init__.py
│       ├── fetch_jobs.py
│       ├── deduplicate.py
│       ├── score_filter.py
│       └── format_report.py
├── fetchers/
│   ├── __init__.py
│   ├── base.py
│   ├── greenhouse.py
│   ├── lever.py
│   └── html_scraper.py
├── learning/
│   ├── __init__.py
│   ├── graph.py
│   └── nodes/
│       ├── __init__.py
│       ├── load_feedback.py
│       ├── analyze_patterns.py
│       └── update_prompt.py
├── config/
│   ├── companies.yaml        ← populate with full company list from SPEC.md
│   ├── settings.py
│   └── scoring_prompt.txt    ← populate with a reasonable initial prompt
├── notifications/
│   ├── __init__.py
│   └── email.py
├── database/
│   ├── __init__.py
│   └── db.py
├── cli/
│   ├── __init__.py
│   └── log.py
├── data/
│   └── .gitkeep              ← empty placeholder so folder is tracked
├── tests/
│   ├── __init__.py
│   ├── test_fetchers.py
│   ├── test_nodes.py
│   ├── test_graph.py
│   └── fixtures/
│       └── sample_listings.json   ← populate with realistic mock Greenhouse/Lever responses
├── dashboard/
│   └── app.py
├── .env.example              ← populate with all vars from SPEC.md
├── .gitignore                ← must include: .env, data/jobs.db, __pycache__, .mypy_cache, .ruff_cache, *.pyc
├── requirements.txt          ← populate with all runtime deps
├── requirements-dev.txt      ← populate with pytest, ruff, mypy
├── Makefile                  ← all targets as stubs: run, learn, test, lint, apply, outcome, stats, clean-today, check-fetchers, seed-demo
└── README.md                 ← placeholder with project title and "see SPEC.md"

Do not proceed to Phase 2 until I confirm Phase 1 looks correct.